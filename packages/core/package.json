{
  "name": "@hnb/core",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Hand and Brain core: the turn protocol over chess.js, plus the shared client/server wire protocol types.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "chess.js": "^1.0.0-beta.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
